<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Calculateur de subventions REEE</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #000000;
      color: #ffffff;
      display: flex;
      flex-direction: column;
      align-items: center;
      margin: 0;
      padding: 20px;
    }
    h1, h2 {
      color: #0A2E5C; /* Bleu foncé Industrielle Alliance */
    }
    .container {
      background-color: #0A2E5C;
      padding: 20px;
      border-radius: 8px;
      max-width: 400px;
      width: 100%;
      margin-top: 20px;
      color: #ffffff;
    }
    label {
      display: block;
      margin-top: 10px;
      text-align: left;
    }
    input {
      width: 100%;
      padding: 8px;
      margin-top: 5px;
      border: none;
      border-radius: 4px;
    }
    button {
      background-color: #ffffff;
      color: #0A2E5C;
      border: none;
      padding: 10px;
      margin-top: 15px;
      border-radius: 4px;
      font-weight: bold;
      cursor: pointer;
      width: 100%;
    }
    button:hover {
      background-color: #cccccc;
    }
    .result {
      margin-top: 15px;
      font-size: 1.2em;
      font-weight: bold;
      color: #ffffff;
    }
  </style>
</head>
<body>

  <h1>Calculateur de subventions REEE</h1>
  <p>Découvrez combien vous pouvez accumuler grâce aux cotisations mensuelles et aux subventions du gouvernement pour le régime enregistré d’épargne-études (REEE).</p>

  <div class="container">
    <h2>Calculateur</h2>

    <label for="age">Âge de l’enfant :</label>
    <input type="number" id="age" value="5">

    <label for="contribution">Montant de cotisation par mois ($) :</label>
    <input type="number" id="contribution" value="100">

    <label for="subvention">Subvention reçue (%) :</label>
    <input type="number" id="subvention" value="20">

    <button onclick="calculer()">Calculer</button>

    <div class="result" id="resultat"></div>
  </div>

  <div class="container">
    <h2>En savoir plus</h2>
    <form id="contact-form">
      <label for="nom">Nom complet :</label>
      <input type="text" id="nom" name="nom" required>

      <label for="telephone">Numéro de téléphone :</label>
      <input type="tel" id="telephone" name="telephone" required>

      <label for="email">Adresse e-mail :</label>
      <input type="email" id="email" name="email" required>

      <button type="submit">Envoyer</button>
    </form>
  </div>

  <script>
    function calculer() {
      const age = parseInt(document.getElementById('age').value) || 0;
      const contribution = parseFloat(document.getElementById('contribution').value) || 0;
      const subventionPercent = parseFloat(document.getElementById('subvention').value) || 0;

      const anneesRestantes = 18 - age;
      const subvention = contribution * (subventionPercent / 100);
      const totalSubvention = subvention * 12 * anneesRestantes;
      const totalEpargne = (contribution * 12 * anneesRestantes) + totalSubvention;

      document.getElementById('resultat').innerText =
        `Après ${anneesRestantes} ans, votre REEE pourrait atteindre ${totalEpargne.toLocaleString()} $ (dont ${totalSubvention.toLocaleString()} $ de subventions).`;
    }

    document.getElementById("contact-form").addEventListener("submit", function(e){
      e.preventDefault();
      alert("Formulaire envoyé ! (Connectez-le à Make ou Google Sheets pour récupérer les données)");
      this.reset();
    });
  </script>

</body>
</html>
